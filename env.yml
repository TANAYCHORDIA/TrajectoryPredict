name: trajpredict
channels:
  - pytorch
  - nvidia
  - conda-forge
dependencies:
  - python=3.10
  - pytorch=2.1
  - pytorch-cuda=11.8
  - pip
  - pip:
      - nuscenes-devkit
      - wandb
      - matplotlib
      - numpy
