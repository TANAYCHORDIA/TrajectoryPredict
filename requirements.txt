# TrajectoryPredict — pip dependencies
# ─────────────────────────────────────────────────────────────────
# KAGGLE USERS (ML Lead, ML #2, Integration Engineer):
#   Run in notebook cell: !pip install -r requirements.txt
#   Do NOT install torch — Kaggle pre-installs it with CUDA support.
#   Reinstalling torch here will silently break GPU access.
#
# LOCAL / VSCODE USERS (Data Engineer):
#   Step 1: conda env create -f environment.yml
#   Step 2: conda activate trajpredict
#   Step 3: pip install --no-cache-dir torch==2.11.0 torchvision==0.26.0 torchaudio==2.11.0
#   Step 4: pip install --no-cache-dir -r requirements.txt
#
# Tested on: Python 3.11, CUDA 13.0, RTX 3050, Ubuntu, March 2026
# ─────────────────────────────────────────────────────────────────

# Data
nuscenes-devkit==1.2.0
numpy==1.26.4
pandas==2.2.3
scipy==1.13.1

# ML Utilities
scikit-learn==1.7.2
tqdm==4.67.3

# Visualization
matplotlib==3.10.8
opencv-python-headless==4.11.0.86
pillow==12.1.1

# Experiment Tracking
wandb==0.25.1

# nuScenes internal dependencies (pinned for stability)
pyquaternion==0.9.9
shapely==2.0.7
descartes==1.1.0